# تقرير الإدخال المباشر للدرجات — لجميع الطلبة بدون استثناء

## المشكلة

في واجهة «تسجيل الدرجات»، كان الطلبة الذين لديهم سجل محفوظ مسبقاً — وعلى رأسهم الطلبة
ضمن فترة السماح الذين يُنشأ لهم سجل تلقائي بحالة «ضمن فترة السماح» — يظهرون بصفوف
مقفلة. لكتابة الدرجة كان لا بد من الضغط على زر «تعديل» أولاً ليفتح الصف، ثم إدخال
الدرجة يدوياً. هذه الخطوة الإضافية تُبطئ الإدخال السريع لدرجات صف كامل، خاصة أن
الخادم نفسه لا يطلب أي خطوة خاصة: أي درجة رقمية تُرسل لطالب ضمن السماح تنهي السماح
ذرياً وتُحتسب من نفس العملية.

## السبب الجذري

- `isGradeEntered` يعتبر حالة «ضمن فترة السماح» (و«قبل تسجيل الطالب» و«غائب» و«غش»
  و«مجاز») درجة «مدخلة».
- حساب `rowLocked` في الصف كان: `entered && !protectedNumericCapture &&
  !editableRows[id]` — أي أن كل صف له سجل محفوظ يُقفل حتى يُضغط «تعديل»، ما لم يكن
  الطالب مجازاً أو الامتحان سابقاً لتسجيله أو مفصولاً (هؤلاء كانوا يستفيدون من
  «الالتقاط الرقمي المحمي» ولا يواجهون القفل أصلاً).
- نتيجة ذلك: الطالب ضمن السماح وحده كان الضحية الأساسية للقفل، رغم أن عقد الخادم
  (`syncAcademicGradeWriteback`) يدعم إنهاء السماح تلقائياً عند استلام درجة رقمية.

## الإصلاح

تعديلات على `src/components/teacher-pro/grade-entry.tsx` فقط:

1. **إزالة القفل نهائياً**: حُذف حساب `rowLocked` وكل ما بُني عليه
   (`numericInputDisabled` / `structuredControlsDisabled` / `notesInputDisabled`
   صارت تعتمد فقط على صلاحيات حقيقية: `canEditPersistedGrade` و
   `protectedNumericCapture` — أي يبقى المؤرشف مقفلاً والمفصول وفق سياسة الالتقاط
   المعلق كما هي).
2. **إزالة زر «تعديل»**: زر فتح الصف المقفل لم يعد له معنى، فحُذف مع منطقه، ويبقى
   زر «حفظ الآن» والحفظ التلقائي عند الخروج (blur) أو Enter لكل الصفوف.
3. **سلسلة التنقل Tab**: أصبحت تشمل كل الصفوف القابلة للإدخال مباشرة (بما فيهم
   ضمن السماح) لتصفح سريع بالكيبورد: اكتب الدرجة ثم Tab للطالب التالي.
4. **حماية وسم السماح من الحذف العرضي**: بما أن خلايا السماح أصبحت قابلة للكتابة،
   أُضيف حارس في `autoSaveGrade`: الخروج من خلية «ضمن فترة السماح» أو «قبل تسجيل
   الطالب» دون كتابة رقم لا يحذف السجل (سلوك Tab/blur الطبيعي)، بينما الحذف بحذف
   الرقم يبقى مخصصاً للدرجات الرقمية وحالات غائب/غش الفعلية كما كان.

## ما لم يتغير

- عقد الخادم لم يُمس: الدرجة الرقمية لطالب ضمن السماح تنهي السماح ذرياً وتُحتسب
  (`graceEnded`)، وللطالب قبل تاريخ تسجيله تُقدّم تاريخ التسجيل وتُصفّر السماح
  (`registrationBackdated`)، وللمجاز تنهي الإجازة وتُحتسب (`leaveEndedByGrade`).
- قيود الصلاحيات: المؤرشف لا يزال غير قابل للإدخال، والمفصول يبقى وفق سياسة
  الدرجة المعلقة للمراجعة.
- تحذيرات إعادة التفعيل اليدوي وسياسات الفصل وإلغاء الغياب الجماعي كلها كما هي.

## الاختبارات

- سكربت جديد: `scripts/test-direct-grade-entry-behavior.mjs` ومسجّل في package.json
  (`test:direct-grade-entry`) ومضمّن في حزمة `test:side-effects`.
- نجحت جميع suites ذات الصلة: `test:grade-entry-integrity`,
  `test:grace-period-integrity` (12 تحققاً حياً)، `test:grade-records-integrity`,
  `test:grade-exam-policy-integrity`, `test:preview-save-consistency-integrity`.
- `tsc --noEmit` نظيف، وESLint بلا أخطاء، و`npm run build` (prisma generate +
  next build) يكتمل بنجاح دون أي تغيير في المخطط أو الترحيلات.

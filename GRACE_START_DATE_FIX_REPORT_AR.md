# إصلاح خلل بداية فترة السماح بتاريخ محدد

## الشكوى
المالك أضاف للطالب **علي مرتضى غازي سعد (BIO-1179)** فترة سماح بتاريخ **14-09-2026**، لكن لم تنطبق على النظام: غيابا امتحانات **16-09** و**19-09** بقيا محسوبين عليه وفرصه صفر من 3.

## التشخيص
- سجل التعديل بتاريخ 14-09 (22:43 UTC) نجح كعملية عادية **بدون أي تغيير أكاديمي**:
  `createdAt = 2026-06-16` و`accountingGraceDays = 6` و`gracePeriodStartDate = 2026-06-16` كلها بقيت كما هي.
- السبب الجذري: الواجهة لا تملك حقل تاريخ بداية سماح صريحاً. الحقل الوحيد المؤرخ هو
  «تاريخ إضافة الطالب / بداية السماح» (createdAt) ويقفز به عقبات معاينة الأثر وتأكيده
  وإعادة الحفظ، وأي انقطاع في المسار يُسقط نية التجديد **صامتة** ويحفظ بقية البيانات فقط —
  فيبدو للمالك أنه أضاف سماحاً والنظام يرى تعديلاً بلا أثر سماح.
- كذلك تجديد نفس عدد الأيام يعتمد على علم لمسٍ داخلي قد يتصفّر في مسارات الاسترجاع.

## الإصلاح
### 1) إصلاح بيانات BIO-1179 (منفَّذ فعلياً)
عبر نفس تسلسل حفظ الخادم داخل معاملة serializable:
- `gracePeriodStartDate: 2026-06-16 → 2026-09-14` (المدة 6 أيام بلا تغيير، `gracePeriodEndedAt = null`).
- غيابا 16-09 و19-09 حُوّلا إلى **«ضمن فترة السماح»** مع حذف مكالمة مرتبطة.
- إعادة الاحتساب أعادت الفرص: **0/3 → 2/3**.
- غياب 05-09 يبقى محسوباً بشكل صحيح (يسبق تاريخ البداية 14-09).
- AuditLog: «إصلاح بداية فترة السماح يدوياً — حسام خليل».

### 2) ميزة «تاريخ بداية محدد» كإجراء موثوق
- `src/lib/student-grace.ts`: وضع جديد `custom` + `parseGraceStartDateInput` +
  `validateManualGraceStartDate` (لا يسبق تاريخ التسجيل، لا يدخل المستقبل، بتوقيت بغداد).
- `src/app/api/students/update-impact/route.ts`: المعاينة تقبل `gracePeriodStartDate`
  للوضع `custom` وتطبق نفس قواعد التحقق قبل توليد توكن المعاينة.
- `src/app/api/students/route.ts` (PATCH): يقرأ `gracePeriodStartDate` صراحةً من الطلب
  (خارج قائمة الحقول غير القابلة للكتابة العامة)، يتحقق منه، ويستخدمه كبداية السماح
  ضمن مسار المعاينة والتأكيد الموجود، ويعيد فتح السماح المنتهي بدرجة رقمية سابقة.
- `src/components/teacher-pro/student-registry.tsx`: قسم «التسجيل وفترة السماح» يضيف
  قائمة **«بداية فترة السماح»**: من تاريخ التسجيل / من يوم حفظ التعديل / **تاريخ محدد**
  مع منتقي تاريخ، وتحقق فوري بالواجهة، ودخول كامل عبر معاينة الأثر وتأكيده قبل الحفظ.
- الاختبارات: `test:grace-period-integrity` أضيف لها فحص الميزة الجديدة، وتحديث فحصين
  قديمين كانا يعكسان حالة سابقة (اسم migration القديم، روابط t.me قبل tg://).

## ما لم يتغير
- المخطط ولا الترحيلات؛ عقد الحفظ المحمي بالمعاينة والتوكن؛ سلوك تعديل الاسم/الهاتف
  لا يعيد بدء السماح؛ الأيام التلقائية الثلاث للطالب الجديد؛ سياسة إنهاء السماح بدرجة رقمية.

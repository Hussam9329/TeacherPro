# تقرير حذف ميزة «فك ارتباط تيليجرام» بالكامل

## الطلب
حذف ميزة فك ارتباط تيليجرام من صفحة سجل الطلاب نهائياً لعدم الحاجة إليها، مع عدم بقاء أي جذور لها في المشروع، ثم التنفيذ والرفع.

## ما تم حذفه (لا شيء تبقى منها)

| الموقع | ما تم حذفه |
|---|---|
| `src/app/api/students/unlink-telegram/route.ts` | مسار API كامل (POST) مع حذف المجلد نهائياً من المشروع |
| واجهة سجل الطلاب `student-registry.tsx` | زر «فك ارتباط تيليجرام»، نافذة تأكيد الحذف بالكامل، نص التوضيح «فك الارتباط يمنع حساب تيليجرام الحالي من الدخول باسم هذا الطالب…»، حالة النافذة، قفل التنفيذ، معالج `handleUnlinkTelegram`، وأيقونة `Unlink` من الاستيرادات |
| طبقة الطلبات `src/lib/api.ts` | الدالة `studentApi.unlinkTelegram` |
| سياسة إعادة تشغيل الطفرات `src/lib/mutation-replay-policy.ts` | إدخال `/api/students/unlink-telegram` من المجموعات الآمنة |
| مسار تحديث الطالب `src/app/api/students/route.ts` | الحارس الذي كان يحظر مسح حقل التيليجرام برسالة «استخدم زر فك ارتباط تيليجرام…» — رسالة تشير لزر لم يعد موجوداً |
| اختبارات السلامة القديمة | 4 فحوصات كانت تتحقق من وجود الميزة، واستُبدلت بفحوصات تتحقق من حذفها الكامل |

## كيف يتم فك الارتباط الآن (بديل آمن دون أي زر)

مسح حقل «معرف التيليجرام» من نافذة تعديل الطالب وحفظ التعديل يقوم بفك الارتباط كاملاً وبتعامل آمن:

1. التعديل على حقل التيليجرام حصري لمدير النظام (بوابة 403 القائمة سلفاً تبقى كما هي).
2. عند المسح، يُرسل النموذج `telegram: ""` ويُعاد حساب `telegramKey` تلقائياً إلى `null` داخل مسار التحديث (`data.telegramKey = identityKeys.telegramKey`)، فلا يبقى أي أثر يمكّن حساب تيليجرام القديم من الدخول باسم الطالب عبر البوت.
3. أرقام الهاتف (رقم الطالب ورقم ولي الأمر) لا تتأثر إطلاقاً.
4. العملية تتم داخل نفس معاملة تحديث الطالب الذرية مع سجل التدقيق الموجود أصلاً، دون أي مسار أو صلاحية إضافية.
5. الحقل يبقى قابلاً للاستخدام لربط حساب تيليجرام جديد لاحقاً.

## الاختبارات

- سكربت جديد `scripts/test-telegram-unlink-removal.mjs` (10 فحوصات) يتحقق من:
  - حذف مجلد المسار نهائياً.
  - عدم وجود أي ذكر للميزة في أي ملف شيفرة داخل `src` (مسح شامل لكل الملفات).
  - خلوّ طبقة الطلبات وسياسة إعادة التشغيل من أي أثر.
  - بقاء فك الارتباط عبر مسح الحقل يعمل بأمان مع حصره بالمدير.
  - تسجيل الاختبار في `package.json` وسلسلة `test:side-effects`.
- تحديث `scripts/test-student-registry-integrity.mjs`: حذف فحوصات الميزة القديمة وإضافة فحصَي الإزالة الكاملة.
- تحديث `scripts/test-student-registry-behavior.mjs`: إعادة تسمية اختبار كان يحمل اسم الميزة المحذوفة.
- النتائج: 10/10 لاختبار الإزالة، وكل اختبارات سجل الطلاب (سلامة + سلوك) ناجحة، و`tsc --noEmit` نظيف، والبناء الإنتاجي ناجح، واختبارات السلاسل المرتبطة (السماح، الدرجات، النقل، الامتحانات، الاسترجاع، النشر) كلها ناجحة.

## ملاحظات

- لم يُمسّ أي شيء آخر متعلق بتيليجرام: ربط الحساب عبر البوت، دخول الطلاب، وحقلا `telegram`/`telegramKey` في قاعدة البيانات كما هي.
- خطأ ESLint المسبق `react-hooks/refs` في `openDeleteDialog` موجود في الكود قبل هذا التغيير (على الكوميت الأساس) ولم يتم المساس به عمداً ليبقى التغيير محصوراً في نطاق الطلب.

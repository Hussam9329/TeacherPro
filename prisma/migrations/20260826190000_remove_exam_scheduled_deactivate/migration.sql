-- إزالة ميزة «تعطيل مجدول» من نظام الامتحانات نهائياً.
-- لا يوجد أي امتحان في قاعدة البيانات يستخدم هذا العمود (تم التحقق: 0 صفوف)،
-- لذا حذف العمود آمن ولا يحتاج ترحيل بيانات.
ALTER TABLE "Exam" DROP COLUMN IF EXISTS "scheduledDeactivateAt";
